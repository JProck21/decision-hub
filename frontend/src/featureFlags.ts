/**
 * Feature flags for toggling UI elements.
 *
 * Set SHOW_GITHUB_BUTTONS to true once the repo is public.
 */
export const SHOW_GITHUB_BUTTONS = true;

/** When true, GitHub links on skill detail pages point to the SKILL.md file
 *  (via manifest_path) instead of the repo root. */
export const LINK_TO_MANIFEST = false;
